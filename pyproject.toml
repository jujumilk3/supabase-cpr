[project]
name = "supabase-cpr"
version = "0.1.0"
description = "Supabase Database Keep-Alive Service - Prevents database from sleeping by periodic table inserts"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "psycopg2-binary>=2.9.9",
]
